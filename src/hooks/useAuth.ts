/**
 * @deprecated Use `import { useAuth } from '@/lib/auth-context'` or `import { useAuth } from '@/hooks'`
 * This file is kept to avoid breaking any direct imports during migration.
 * It simply re-exports from the canonical context.
 */
export { useAuth } from '@/lib/auth-context'